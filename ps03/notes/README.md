# PS03 experiment notes

Record actual output here while running the experiments. Keep screenshots in
`ps03/results/`.

## Linux commands

- OS, architecture, compiler, and process limit:
- `ps` versus `top`:
- `nice` / `renice` values:
- `kill` command and result:

## Shell jobs

- `ss1_1.sh` foreground timing:
- `ss1_1.sh` background behavior:
- `Ctrl+Z` and `jobs -l` result:
- `fg` behavior:
- `bg` behavior:

## GUI tools

- Linux tool, package, and version:
- Linux values monitored:
- Windows tool and version:
- Windows values monitored:

## Scheduler

```bash
./bin/PS3 case1.csv
./bin/PS3 case2.csv
./bin/PS3 case3.csv
```

Copy the exact outputs and note the corresponding screenshot filename.
